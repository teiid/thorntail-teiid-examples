swarm:
  datasources:
    data-sources:
      accounts-ds:
        driver-name: h2
        connection-url: jdbc:h2:mem:test;INIT=runscript from 'customer-schema.sql';
        user-name: sa
        password: sa
